[project]
name = "maya-mcp"
version = "1.0.0"
description = "MCP-Server fuer Autodesk Maya 2026 (stdio) mit Listener im laufenden Maya"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=2.1.1",
]

[tool.uv]
package = false
